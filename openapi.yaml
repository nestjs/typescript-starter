openapi: 3.0.0
info: 
  title: AutoBetter API
  version: 0.0.1
paths: 
  /bet/: 
    get: 
      parameters: 
      - name: date
        in: path
        required: true
        schema: 
          $ref: '#/components/schemas/date'
      responses: 
        '200':
          description: Latest bet
          content:
            application/json:
              schema: 
                $ref: '#/components/schemas/bet' 
    post: 
      parameters: 
      - name: date
        in: path
        required: true
        schema: 
          $ref: '#/components/schemas/date'
      requestBody:
        description: Body
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/bet'
      responses: 
        '200':
          description: Latest bet
          content:
            application/json:
              schema: 
                $ref: '#/components/schemas/bet' 
  /bet/{date}: 
    get: 
      parameters: 
      - name: date
        in: path
        required: true
        schema: 
          $ref: '#/components/schemas/date'
      responses: 
        '200':
          description: Latest bet
          content:
            application/json:
              schema: 
                $ref: '#/components/schemas/bet' 
components:
  schemas: 
    date: 
      type: string
      format: datetime
    bet:
      type: object
      properties:
        id:
          type: integer
        date:
          $ref: '#/components/schemas/date'
        course_name:
          type: string
        number:
          type: integer
        url:
          type: string
          format: url
        odds:
          type: integer
        ev:
          type: number
        placed:
          type: boolean
        